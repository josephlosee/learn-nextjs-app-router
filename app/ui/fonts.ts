import { Inter, Rubik_Glitch, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const glitch = Rubik_Glitch({ subsets: ['latin'], weight: '400' });
export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });
export const primary_font = lusitana;
